import os
print(os.listdir())
# modules - it is a code written by someone else but can be used by other by importing it
# 1. Built in modules - already available in python( no need to install them separately)
# 2. External modules- need to install them by using package manager (pip)